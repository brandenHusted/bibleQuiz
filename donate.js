// merge two arrays together
var merge = function(nums1, m, nums2, n) {
    for (let i=m, j=0; j< n; i++, j++){
        nums1[i] = nums2[j];
    }
    nums1.sort(a,b);
}

// Do not know what to put here yet although I am thinking of sucuring this page more like a test to see if your human of some way to add security for donating